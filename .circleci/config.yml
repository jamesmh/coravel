version: 2
jobs:
  build:
    working_directory: ~/coravel/Src
    docker: 
      - image: microsoft/dotnet:2.2-sdk
    steps:
      - checkout
      - run: 'dotnet build ./All.sln'
      - run: 'dotnet test ./All.sln'
